# PDCP System Architecture & Service Map

```
┌─────────────────────────────────────────────────────────────────┐
│                   PDCP Microservices Platform                    │
│              Post-Dispatch Consolidation Platform                │
└─────────────────────────────────────────────────────────────────┘

                         🌐 EXTERNAL CLIENTS
                                │
                ┌───────────────┼───────────────┐
                │               │               │
            Mobile         Web Browser       API Clients
            (Drivers)     (Dispatchers)     (Partners)
                │               │               │
                └───────────────┼───────────────┘
                                │
                ┌───────────────▼────────────────┐
                │      API GATEWAY (3000)        │
                │  - Route requests              │
                │  - Authentication              │
                │  - Rate limiting               │
                └───────────┬────────┬───────────┘
                            │        │
         ┌──────────────────┴────────┴───────────────────┐
         │                                               │
    ┌────▼─────────┐  ┌────────────────────────┐  ┌─────▼──────┐
    │ DECISION      │  │ VEHICLE TRACKING       │  │ AUTH        │
    │ ENGINE (3001) │  │ SERVICE (3002)         │  │ SERVICE     │
    │               │  │                        │  │ (3003)      │
    │ - Algorithms  │  │ - GPS tracking         │  │             │
    │ - Scoring     │  │ - Route optimization   │  │ - JWT       │
    │ - ML Models   │  │ - Geofencing           │  │ - Users     │
    └────────────┬──┘  └──────┬─────────────────┘  └─────┬───────┘
                 │            │                         │
                 │    ┌────────┴──────┬──────────────────┤
                 │    │               │                  │
         ┌───────▼────▼┐  ┌───────┬───▼──────┐  ┌─────────────────┐
         │ PARCEL      │  │       │          │  │ CUSTODY         │
         │ MANAGEMENT  │  │       │          │  │ SERVICE (3005)  │
         │ (3004)      │  │       │ SHARED   │  │                 │
         │             │  │       │ DATABASE │  │ - Blockchain    │
         │ - Lifecycle │  │       │          │  │ - Immutable log │
         │ - Tracking  │  │  ┌────▼─┐   ┌──▼──┴──────────────┐   │
         │ - Assignment│  │  │      │   │                    │   │
         └─────────────┘  │  │      │   │  ANALYTICS (3006)  │   │
                          │  │      │   │  AUDIT (3007)      │   │
                          │  └──────┴───┴────────────────────┘   │
                          │                                       │
         ┌────────────────▼────────────────────────────────────┐  │
         │            INFRASTRUCTURE LAYER                     │  │
         │                                                     │  │
         │  ┌──────────────────────┐  ┌──────────────────┐   │  │
         │  │   PostgreSQL (5432)  │  │  Redis (6379)    │   │  │
         │  │                      │  │                  │   │  │
         │  │  - Main Database     │  │  - Cache Layer   │   │  │
         │  │  - Business Data     │  │  - Sessions      │   │  │
         │  │  - User Data         │  │  - Pub/Sub       │   │  │
         │  │  - Analytics Data    │  │  - Queues        │   │  │
         │  └──────────────────────┘  └──────────────────┘   │  │
         │                                                     │  │
         └─────────────────────────────────────────────────────┘  │
                                                                   │
         ┌──────────────────────────────────────────────────────┐ │
         │            SHARED PACKAGES                            │ │
         │                                                       │ │
         │  ┌────────────────┐  ┌──────────────────────────┐   │ │
         │  │ @pdcp/types    │  │ @pdcp/shared            │   │ │
         │  │                │  │                          │   │ │
         │  │ - Interfaces   │  │ - Common utilities       │   │ │
         │  │ - DTOs         │  │ - Middleware             │   │ │
         │  │ - Enums        │  │ - Decorators             │   │ │
         │  │ - Constants    │  │ - Error handlers         │   │ │
         │  └────────────────┘  └──────────────────────────┘   │ │
         │                                                       │ │
         └───────────────────────────────────────────────────────┘ │
                                                                   │
         ┌─────────────────────────────────────────────────────────┐
         │  📊 MONITORING & LOGGING                                │
         │  - Health checks                                        │
         │  - Performance metrics                                  │
         │  - Error tracking                                       │
         │  - Audit logs                                           │
         └─────────────────────────────────────────────────────────┘
```

## Service Communication Flow

```
User Request Flow:
━━━━━━━━━━━━━━━━━

Client
  │
  └──POST /api/consolidate────────→ API Gateway (3000)
                                        │
                        ┌───────────────┼───────────────┐
                        │               │               │
                    1. Authenticate   2. Validate    3. Route
                        │               │               │
                        ▼               ▼               ▼
                  Auth Service      Types Check    Decision Engine
                    (3003)           (Shared)         (3001)
                        │               │               │
                        └───────────────┼───────────────┘
                                        │
                                     Database
                                        │
                                    Response
                                        │
                                      Client


Data Persistence Flow:
━━━━━━━━━━━━━━━━━━━━━

Service A ──┐
             │      ┌──────────────┐
Service B ───┼─────▶│  PostgreSQL  │
             │      │   (Primary)  │
Service C ──┘      └──────────────┘
                           │
                           ▼
                    ┌──────────────┐
                    │   Backup     │
                    │   & Archive  │
                    └──────────────┘


Caching & Performance:
━━━━━━━━━━━━━━━━━━━

Request
  │
  ├─ Check Redis Cache (3379)
  │       │
  │       ├─ HIT  ──→ Return
  │       │
  │       └─ MISS ─┐
  │               │
  │               ▼
  └─ Query Database ─────────┐
                             │
                             ▼
                        Store in Cache
                             │
                             ▼
                          Return
```

## Port Allocation Map

```
┌────────────┬────────────┬──────────────────────┐
│ Service    │ Port       │ Purpose              │
├────────────┼────────────┼──────────────────────┤
│ API Gw     │ 3000       │ Main entry point     │
│ Decision   │ 3001       │ Core algorithms      │
│ Vehicle    │ 3002       │ Tracking system      │
│ Auth       │ 3003       │ Security & tokens    │
│ Parcel     │ 3004       │ Package management   │
│ Custody    │ 3005       │ Blockchain/audit     │
│ Analytics  │ 3006       │ Reports & metrics    │
│ Audit      │ 3007       │ Compliance logging   │
│ PostgreSQL │ 5432       │ Primary data store   │
│ Redis      │ 6379       │ Caching layer        │
└────────────┴────────────┴──────────────────────┘
```

## Deployment Topology

```
                    Internet
                       │
                   ┌───▼───┐
                   │  LB   │  Load Balancer
                   └───┬───┘
                       │
        ┌──────────────┼──────────────┐
        │              │              │
    ┌───▼───┐      ┌──▼────┐     ┌──▼────┐
    │ Pod 1 │      │ Pod 2 │     │ Pod 3 │  Kubernetes Pods
    │       │      │       │     │       │
    │ ┌─────▼──┐   │ ┌─────▼──┐  │ ┌────▼──┐
    │ │Services│   │ │Services│  │ │Service│
    │ │(all 8) │   │ │(all 8) │  │ │(all 8)│
    │ └────┬───┘   │ └────┬───┘  │ └───┬───┘
    │      │       │      │      │     │
    └──────┼───────┴──────┼──────┴─────┘
           │              │
        ┌──▼──────────────▼───┐
        │  Shared Storage      │
        │  PostgreSQL Cluster  │
        │  Redis Cluster       │
        └─────────────────────┘
```

## Data Model Overview

```
┌─────────────────────────────────────────────────────┐
│ PDCP Data Schema                                    │
└─────────────────────────────────────────────────────┘

Users
├── Drivers
├── Dispatchers
└── Admins

Vehicles
├── Active Routes
├── Capacity
└── GPS Location

Parcels
├── Source/Destination
├── Weight/Dimensions
├── Status
└── Customer Info

Consolidation Decisions
├── Algorithm Score
├── Cost Savings
├── Carbon Reduction
└── Confidence Level

Orders
├── Pickup/Delivery
├── Timeline
├── Special Requirements
└── Status History

Blockchain Records
├── Immutable Logs
├── Custody Chain
└── Audit Trail
```

## API Response Flow

```
Request: GET /api/vehicles

      ┌─────────────────────────────┐
      │ API Gateway (3000)          │
      │ - Rate Limit Check          │
      │ - JWT Validation            │
      └────────────┬────────────────┘
                   │
      ┌────────────▼────────────────┐
      │ Vehicle Tracking Service    │
      │ - Business Logic            │
      │ - Data Validation           │
      └────────────┬────────────────┘
                   │
      ┌────────────▼────────────────┐
      │ PostgreSQL Query            │
      │ - Fetch vehicle data        │
      └────────────┬────────────────┘
                   │
      ┌────────────▼────────────────┐
      │ Response Builder            │
      │ - Format JSON               │
      │ - Include metadata          │
      └────────────┬────────────────┘
                   │
      ┌────────────▼────────────────┐
      │ Cache Response (Redis)      │
      │ - 5 minute TTL              │
      └────────────┬────────────────┘
                   │
      ┌────────────▼────────────────┐
      │ Return to Client            │
      │ 200 OK + JSON Body          │
      └─────────────────────────────┘
```

---

## Quick Navigation

| Component | Location | Purpose |
|-----------|----------|---------|
| **Services** | `apps/` | 8 NestJS microservices |
| **Database** | `docker/postgres/` | SQL schemas & init |
| **Cache** | `docker/redis/` | Redis configuration |
| **Shared** | `packages/` | Common types & utilities |
| **Config** | `docker-compose.yml` | Infrastructure setup |
| **Scripts** | `scripts/` | Build & deployment |

---

**To start this system:**
```bash
node run-complete-project.js
```
